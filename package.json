{
  "name": "folkrobot2",
  "version": "0.0.1",
  "dependencies": {
    "bower": "^1.7.7",
    "coffee-script": "^1.10.0",
    "express": "^4.13.4"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "bower install"
  }
}
